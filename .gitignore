# Compiled binaries
bin/
jetmon2

# Editor and OS files
.DS_Store
.idea/
*.swp
*.swo

# Secrets and local config
.env
config/config.json
config/db-config.conf

# Generated TLS certificates
certs/*.crt
certs/*.key

# Generated veriflier runtime config (veriflier-sample.json is tracked)
veriflier2/config/veriflier.json

# Legacy C++ veriflier build/runtime artifacts
veriflier/*.o
veriflier/moc_*.cpp
veriflier/moc_predefs.h
veriflier/.qmake.stash
veriflier/veriflier
veriflier/logs/*.log

# Generated protobuf Go stubs (produced by `make generate`)
*.pb.go

# Runtime output dirs
logs/*.log
stats/*
!logs/.gitkeep
!stats/.gitkeep

# Go test coverage output
coverage.out
coverage.html

# AI tool directories
.codex

# Local Claude settings (project settings.json is tracked)
.claude/settings.local.json

# Local Docker development overrides
docker/.env
docker/docker-compose.override.yml
